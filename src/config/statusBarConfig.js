import {StatusBar} from 'react-native'

// StatusBar do Android (onde fica os icones de bateria, relogio, wifi etc) 
StatusBar.setBackgroundColor('#7159c1');

// IOS
StatusBar.setBarStyle("light-content");