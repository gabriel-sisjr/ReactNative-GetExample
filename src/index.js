import React from 'react'
import Routs from './routes'

// Importando status bar, já que nao vamos utilizar em local nenhum, n precisa nomear
import './config/statusBarConfig'

const App = () => <Routs></Routs>

export default App;